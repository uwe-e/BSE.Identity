@inject NavigationManager UriHelper

@code {
    protected override void OnInitialized()
    {
        UriHelper.NavigateTo("Identity/Account/Login1", true);
        //UriHelper.NavigateTo("Login", true);
    }
}
